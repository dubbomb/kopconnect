# Real news aggregation. At least one provider is recommended.
NEWS_API_KEY=
GUARDIAN_API_KEY=

# Comma-separated publisher RSS feeds you are authorised to use.
CUSTOM_RSS_FEEDS=

# football-data.org v4 key. Liverpool team ID defaults to 64.
FOOTBALL_DATA_API_KEY=
FOOTBALL_DATA_TEAM_ID=64

# Optional public site URL used in metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
